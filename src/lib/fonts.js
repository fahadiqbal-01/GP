import { Open_Sans, Inter } from "next/font/google";

export const inter = Inter({
  weight: "700",
  subsets: ["latin"],
});
export const interSec = Inter({
  weight: "400",
  subsets: ["latin"],
});

export const openSans = Open_Sans({
  subsets: ["latin"],
});
